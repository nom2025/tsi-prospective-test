# プロジェクトファイル・フォルダ整理計画

**作成日**: 2026年1月4日

## 📋 整理の目的

プロジェクト配下に散在しているファイル・フォルダを整理し、プロジェクト構造を明確にします。

## 🔍 整理対象

### 1. ルート直下の作業記録ファイル → `docs/` に移動

以下の作業記録ファイルを `docs/` フォルダに移動します：

- `作業記録_GNSS統合とM7.5前兆強化.md` → `docs/作業記録_GNSS統合とM7.5前兆強化.md`
- `作業記録_火山データ取得検討.md` → `docs/作業記録_火山データ取得検討.md`

**理由**: 作業記録はドキュメント類として `docs/` フォルダで一元管理する

### 2. 一時的な日付フォルダ → `データ自動取得/src/archive/` に移動

以下の日付別作業フォルダをアーカイブフォルダに移動します：

- `データ自動取得/src/20251221/` → `データ自動取得/src/archive/20251221/`
- `データ自動取得/src/20251223/` → `データ自動取得/src/archive/20251223/`
- `データ自動取得/src/20251226/` → `データ自動取得/src/archive/20251226/`
- `データ自動取得/src/20251229/` → `データ自動取得/src/archive/20251229/`
- `データ自動取得/src/20251231/` → `データ自動取得/src/archive/20251231/`
- `データ自動取得/src/20260104/` → `データ自動取得/src/archive/20260104/`

**理由**: 日付別の作業フォルダは一時的なもので、アーカイブフォルダにまとめて管理する

### 3. ルート直下のバッチファイル → そのまま保持

以下のバッチファイルはルート直下に保持します（メイン実行ファイルとして）：

- `データ取得とスコア計算実行.bat` - 完全実行用（保持）
- `データ取得実行.bat` - データ取得のみ実行用（保持）
- `スコア計算実行.bat` - スコア計算のみ実行用（保持）
- `install_packages.bat` - パッケージインストール用（保持）

**理由**: メイン実行ファイルとして、ルートから実行できるようにする

### 4. その他のファイル・フォルダ → そのまま保持

以下のファイル・フォルダはそのまま保持します：

- `ロジック見直し/` - ロジック改善の検討フォルダ（途中段階の作業）
- `新データ検討/` - GPS・気圧データの検討フォルダ（途中段階の作業）
- `グラフ/old/` - 古いグラフのバックアップ
- `データ自動取得/debug/` - デバッグ用HTMLファイル
- `新データ検討/debug/` - デバッグ用HTMLファイル

**理由**: 途中段階の作業やデバッグファイルは必要に応じて参照するため保持

## 📁 整理後の構造

```
52-2_cursor_地震/
├── docs/                          # ドキュメント類（整理後）
│   ├── README.md
│   ├── プロジェクト状態.md
│   ├── 特徴探索ガイド.md
│   ├── 整理計画.md
│   ├── 作業記録_GNSS統合とM7.5前兆強化.md  ← 移動
│   └── 作業記録_火山データ取得検討.md      ← 移動
│
├── データ自動取得/
│   ├── src/
│   │   ├── archive/               # アーカイブフォルダ（新規作成）
│   │   │   ├── 20251221/          ← 移動
│   │   │   ├── 20251223/          ← 移動
│   │   │   ├── 20251226/          ← 移動
│   │   │   ├── 20251229/          ← 移動
│   │   │   ├── 20251231/          ← 移動
│   │   │   └── 20260104/          ← 移動
│   │   ├── fetch_depth_data.py
│   │   ├── fetch_earthquake_data.py
│   │   ├── fetch_world_earthquake_data.py
│   │   └── fetch_gnss_data.py
│   ├── data/
│   ├── debug/
│   └── README.md
│
├── ロジック見直し/                 # そのまま保持
├── 新データ検討/                   # そのまま保持
├── グラフ/                         # そのまま保持
│   └── old/                        # そのまま保持
│
├── データ取得とスコア計算実行.bat   # そのまま保持
├── データ取得実行.bat               # そのまま保持
├── スコア計算実行.bat               # そのまま保持
├── install_packages.bat             # そのまま保持
└── 整理スクリプト.py                # 整理用スクリプト
```

## 🚀 実行方法

### 方法1: 整理スクリプトを実行（推奨）

**注意**: PowerShellターミナルからの実行はパースエラーが発生する可能性があります。以下のいずれかの方法で実行してください：

1. **Windowsエクスプローラーから実行**:
   - `整理スクリプト.py` を右クリック → 「Pythonで開く」

2. **コマンドプロンプト（cmd）から実行**:
   ```cmd
   cd /d "C:\Users\arumo\OneDrive\デスクトップ\学習_AI\40_AI駆動開発\52-2_cursor_地震"
   python 整理スクリプト.py
   ```

3. **Windows Terminalから実行**:
   ```bash
   cd "C:\Users\arumo\OneDrive\デスクトップ\学習_AI\40_AI駆動開発\52-2_cursor_地震"
   python 整理スクリプト.py
   ```

### 方法2: 手動で整理

1. **作業記録ファイルの移動**:
   - `作業記録_GNSS統合とM7.5前兆強化.md` を `docs/` に移動
   - `作業記録_火山データ取得検討.md` を `docs/` に移動

2. **アーカイブフォルダの作成**:
   - `データ自動取得/src/archive/` フォルダを作成

3. **日付フォルダの移動**:
   - `データ自動取得/src/20251221/` を `データ自動取得/src/archive/20251221/` に移動
   - `データ自動取得/src/20251223/` を `データ自動取得/src/archive/20251223/` に移動
   - `データ自動取得/src/20251226/` を `データ自動取得/src/archive/20251226/` に移動
   - `データ自動取得/src/20251229/` を `データ自動取得/src/archive/20251229/` に移動
   - `データ自動取得/src/20251231/` を `データ自動取得/src/archive/20251231/` に移動
   - `データ自動取得/src/20260104/` を `データ自動取得/src/archive/20260104/` に移動

## ✅ 整理後の確認事項

整理後、以下の点を確認してください：

1. **作業記録ファイル**: `docs/` フォルダに移動されているか
2. **アーカイブフォルダ**: `データ自動取得/src/archive/` に日付フォルダが移動されているか
3. **バッチファイル**: ルート直下のバッチファイルが正常に動作するか
4. **データ取得スクリプト**: 日付フォルダの移動後も正常に動作するか（新しい日付フォルダは `src/` 直下に作成される）

## 📝 注意事項

1. **バックアップ**: 整理前に重要なファイルのバックアップを取ることを推奨します
2. **パス参照**: スクリプト内のパス参照は相対パスを使用しているため、整理後も正常に動作するはずです
3. **途中段階の作業**: `ロジック見直し/` と `新データ検討/` は途中段階の作業フォルダのため、そのまま保持します
4. **デバッグファイル**: `debug/` フォルダはデバッグ時に必要になる可能性があるため、そのまま保持します

## 🔄 今後の整理

将来的に以下の整理も検討できます：

1. **途中段階の作業フォルダの整理**:
   - `ロジック見直し/` と `新データ検討/` の内容を本番環境に統合するか、アーカイブする
   
2. **デバッグファイルの整理**:
   - 古いデバッグファイルを定期的に削除する

3. **バッチファイルの整理**:
   - バッチファイルを `scripts/` フォルダにまとめる（ただし、ルートからの実行性を維持する）

---

## ✅ 整理完了記録

**整理実施日**: 2026年1月4日  
**整理方法**: 手動整理

### 整理完了項目

#### 1. 作業記録ファイルの移動 ✅
- ✅ `作業記録_GNSS統合とM7.5前兆強化.md` → `docs/作業記録_GNSS統合とM7.5前兆強化.md`（移動完了）
- ✅ `作業記録_火山データ取得検討.md` → `docs/作業記録_火山データ取得検討.md`（移動完了）
- ✅ ルート直下の作業記録ファイルは全て削除済み

#### 2. 日付フォルダのアーカイブ ✅
- ✅ `データ自動取得/src/archive/` フォルダを作成
- ✅ `20251221/` → `archive/20251221/`（移動完了）
- ✅ `20251223/` → `archive/20251223/`（移動完了）
- ✅ `20251226/` → `archive/20251226/`（移動完了）
- ✅ `20251229/` → `archive/20251229/`（移動完了）

#### 3. 最新の日付フォルダ（保持）
以下の日付フォルダは最新のため、`src/` 直下に保持：
- `20251231/` - 2025年12月31日の作業フォルダ（保持）
- `20260104/` - 2026年1月4日の作業フォルダ（保持）

**理由**: 最新の作業フォルダは使用中の可能性があるため、一定期間後に `archive/` に移動する運用とする

### 整理後の実際の構造

```
52-2_cursor_地震/
├── docs/                          # ドキュメント類（整理完了）
│   ├── README.md
│   ├── プロジェクト状態.md
│   ├── 特徴探索ガイド.md
│   ├── 整理計画.md
│   ├── ファイル整理計画.md         # このファイル
│   ├── 作業記録_GNSS統合とM7.5前兆強化.md  ✅ 移動完了
│   └── 作業記録_火山データ取得検討.md      ✅ 移動完了
│
├── データ自動取得/
│   ├── src/
│   │   ├── archive/               # アーカイブフォルダ（作成済み）
│   │   │   ├── 20251221/          ✅ 移動完了
│   │   │   ├── 20251223/          ✅ 移動完了
│   │   │   ├── 20251226/          ✅ 移動完了
│   │   │   └── 20251229/          ✅ 移動完了
│   │   ├── 20251231/              # 最新フォルダ（保持）
│   │   ├── 20260104/              # 最新フォルダ（保持）
│   │   ├── fetch_depth_data.py
│   │   ├── fetch_earthquake_data.py
│   │   ├── fetch_world_earthquake_data.py
│   │   └── fetch_gnss_data.py
│   └── ...
│
├── ロジック見直し/                 # そのまま保持
├── 新データ検討/                   # そのまま保持
├── グラフ/                         # そのまま保持
│   └── old/                        # そのまま保持
│
├── データ取得とスコア計算実行.bat   # そのまま保持
├── データ取得実行.bat               # そのまま保持
├── スコア計算実行.bat               # そのまま保持
├── install_packages.bat             # そのまま保持
├── 整理スクリプト.py                # 整理用スクリプト
└── 整理実行.bat                     # 整理実行バッチファイル
```

### 今後の運用

1. **日付フォルダの管理**:
   - データ取得スクリプトは新しい日付フォルダを `src/` 直下に作成
   - 一定期間（例: 1週間以上）経過後、古い日付フォルダを `archive/` に移動

2. **作業記録ファイル**:
   - 新しい作業記録ファイルは `docs/` フォルダに直接作成する

3. **定期的な整理**:
   - 月次または四半期ごとに、古い日付フォルダを `archive/` に移動
   - デバッグファイルが増えすぎた場合は、古いものを削除

---

**最終更新**: 2026年1月4日（整理完了記録を追加）

